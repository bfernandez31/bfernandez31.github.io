version: 1
project:
  name: "portfolio"
  language: typescript
  framework: none
runtime:
  manager: bun
commands:
  install: "bun install"
  test: "bun run test"
  lint: "bun run lint"
testing:
  e2e: false
agent:
  cli: claude-code
